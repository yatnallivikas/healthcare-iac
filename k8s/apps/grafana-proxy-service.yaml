apiVersion: v1
kind: Service
metadata:
  name: grafana-proxy
  namespace: healthcare-apps
  labels:
    app.kubernetes.io/name: grafana-proxy
    app.kubernetes.io/component: monitoring
    app.kubernetes.io/part-of: healthcare-platform
spec:
  type: ExternalName
  externalName: observability-grafana.monitoring.svc.cluster.local
  ports:
    - name: http
      port: 80
